import { NzColorDirective } from './nz-color.directive';

describe('GreenButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NzColorDirective();
    expect(directive).toBeTruthy();
  });
});
